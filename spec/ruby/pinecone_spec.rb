# frozen_string_literal: true

RSpec.describe Ruby::Pinecone do
  it "has a version number" do
    expect(Ruby::Pinecone::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
